# Proposed PR summary

Reconcile P4 authority after PAM A and capture the mutation-testing prerequisite-access failure as a prerequisite-sensitive traversal and semantic representation-selection design delta. Add a public-safe regression oracle. No runtime implementation or PAM B/C claim.

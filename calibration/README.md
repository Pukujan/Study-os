# Calibration registry

Human-readable index: `docs/CALIBRATION_INDEX.md`

Machine-readable registry: `manifest.json`

The registry exists so a fresh human or agent can locate calibration evidence without already knowing a session date or directory path.

Do not equate a `golden/` filename with complete raw evidence or runtime authority. Each dataset entry records those statuses separately.
